/* === MARKS — mỗi loại event một màu === */
.tl-mark{position:absolute;top:-3px;bottom:-3px;width:4px;border-radius:3px;
  cursor:pointer;z-index:5;transition:all .2s;transform:translateX(-50%)}
.tl-mark::before{content:'';position:absolute;top:-10px;left:50%;transform:translateX(-50%);
  font-size:11px;pointer-events:none}

/* Loại: start/end */
.tl-mark.ev-start,.tl-mark.ev-end{background:var(--green-400);width:5px}
.tl-mark.ev-start::before{content:'🏁'}
.tl-mark.ev-end::before{content:'🏁'}

/* Loại: boss */
.tl-mark.ev-boss_minor,.tl-mark.ev-boss_major{background:var(--gold);width:5px;
  box-shadow:0 0 6px rgba(251,191,36,.4)}
.tl-mark.ev-boss_minor::before{content:'☀️'}
.tl-mark.ev-boss_major::before{content:'🔥';font-size:13px;top:-12px}

/* Loại: solo PVP */
.tl-mark.ev-solo{background:#f87171;width:5px;box-shadow:0 0 6px rgba(248,113,113,.4)}
.tl-mark.ev-solo::before{content:'⚔️'}

/* loại: custom (người dùng tạo) */
.tl-mark.ev-custom{background:var(--dps);width:3px}
.tl-mark.ev-custom::before{content:'📌';font-size:9px;top:-11px}

/* Mark hover + active */
.tl-mark:hover{filter:brightness(1.3);transform:translateX(-50%) scaleY(1.2)}
.tl-mark.active{filter:brightness(1.4);transform:translateX(-50%) scaleY(1.3);
  box-shadow:0 0 12px currentColor!important}
.tl-mark.active.ev-start,.tl-mark.active.ev-end{box-shadow:0 0 12px rgba(34,197,94,.6)!important}
.tl-mark.active.ev-boss_minor,.tl-mark.active.ev-boss_major{box-shadow:0 0 14px rgba(251,191,36,.7)!important}
.tl-mark.active.ev-solo{box-shadow:0 0 14px rgba(248,113,113,.7)!important}
.tl-mark.active.ev-custom{box-shadow:0 0 12px rgba(96,165,250,.6)!important}

/* Label chi tiết của mark */
.tl-mark .tl-mark-label{position:absolute;bottom:calc(100% + 14px);left:50%;transform:translateX(-50%);
  font-size:9px;font-weight:700;white-space:nowrap;pointer-events:none;
  background:rgba(0,0,0,.9);padding:3px 8px;border-radius:5px;opacity:0;transition:opacity .2s;
  border:1px solid rgba(255,255,255,.15);z-index:10}
.tl-mark.ev-start .tl-mark-label,.tl-mark.ev-end .tl-mark-label{color:var(--green-300);
  border-color:rgba(34,197,94,.3)}
.tl-mark.ev-boss_minor .tl-mark-label,.tl-mark.ev-boss_major .tl-mark-label{color:var(--gold);
  border-color:rgba(251,191,36,.3)}
.tl-mark.ev-solo .tl-mark-label{color:#fca5a5;border-color:rgba(248,113,113,.3)}
.tl-mark.ev-custom .tl-mark-label{color:var(--dps);border-color:rgba(96,165,250,.3)}
.tl-mark:hover .tl-mark-label,.tl-mark.active .tl-mark-label{opacity:1}
/* System marks: luôn hiện thời gian */
.tl-mark .tl-mark-time{position:absolute;top:calc(100% + 4px);left:50%;transform:translateX(-50%);
  font-size:8px;font-weight:700;color:var(--text3);white-space:nowrap;pointer-events:none}
.tl-mark.ev-boss_minor .tl-mark-time,.tl-mark.ev-boss_major .tl-mark-time{color:rgba(251,191,36,.6)}
.tl-mark.ev-solo .tl-mark-time{color:rgba(248,113,113,.5)}
